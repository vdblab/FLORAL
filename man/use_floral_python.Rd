% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pyload.R
\name{use_floral_python}
\alias{use_floral_python}
\title{Configure the Python environment used by FLORAL}
\usage{
use_floral_python(python = NULL, virtualenv = NULL, condaenv = NULL,
  required = TRUE)
}
\arguments{
\item{python}{Character string. Full path to a Python binary, e.g.
\code{"/usr/bin/python3"} or \code{"/opt/homebrew/bin/python3"}.
Mutually exclusive with \code{virtualenv} and \code{condaenv}.}

\item{virtualenv}{Character string. Name or path of a Python virtual
environment created with \code{python -m venv} or
\code{reticulate::virtualenv_create()}.  Mutually exclusive with
\code{python} and \code{condaenv}.}

\item{condaenv}{Character string. Name of a conda environment.  Mutually
exclusive with \code{python} and \code{virtualenv}.}

\item{required}{Logical.  If \code{TRUE} (default), an error is raised when
the requested environment cannot be found.  Set to \code{FALSE} to allow
silent fallback.}
}
\value{
Invisibly returns a named list with fields:
\describe{
  \item{\code{python_available}}{Logical — is Python reachable?}
  \item{\code{torch_available}}{Logical — is the \code{torch} module importable?}
  \item{\code{knockoff_loaded}}{Logical — was \code{knockoff.py} sourced correctly?}
}
}
\description{
FLORAL's knockoff generation relies on PyTorch via
\href{https://rstudio.github.io/reticulate/}{reticulate}.  Because reticulate
locks the Python interpreter as soon as it is first used, the environment
must be configured \emph{before} \code{library(FLORAL)} (or any other call
that would initialise Python).

The recommended workflow is:

\enumerate{
  \item Call \code{use_floral_python()} \strong{once} in your
        \code{.Rprofile} or at the very top of your script, \emph{before}
        \code{library(FLORAL)}.
  \item Load the package normally: \code{library(FLORAL)}.
}

Calling this function \emph{after} Python has already been initialised
will save the preference for the next session but cannot change the active
interpreter; restart R to apply a new setting.
}
\section{Installing dependencies}{

The knockoff script requires \code{torch} and \code{numpy}.  After creating
your environment, install them with:

\preformatted{
# Option A — virtual environment (recommended)
reticulate::virtualenv_create("floral-env")
reticulate::virtualenv_install("floral-env", c("torch", "numpy"))

# Option B — conda
reticulate::conda_create("floral")
reticulate::conda_install("floral", c("pytorch", "numpy"),
                          channel = "pytorch")
}
}

\examples{
\dontrun{
# ---- Place one of these BEFORE library(FLORAL) ----

# Option A: specific Python binary
use_floral_python(python = "/usr/bin/python3")

# Option B: virtual environment (recommended for isolation)
use_floral_python(virtualenv = "floral-env")

# Option C: conda environment
use_floral_python(condaenv = "floral")

# ---- Then load the package as usual ----
library(FLORAL)

# ---- Check everything is working ----
check_python_setup()
}
}
\seealso{
\code{\link{check_python_setup}} for diagnosing the current setup.
}
